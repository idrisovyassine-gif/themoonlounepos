services:
  - type: web
    name: moonlounge-pos
    runtime: node
    rootDir: restaurant-app
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20
